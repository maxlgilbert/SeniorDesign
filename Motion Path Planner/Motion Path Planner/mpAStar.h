//
//  mpAStar.h
//  Motion Path Planner
//
//  Created by Max Gilbert on 2/5/15.
//  Copyright (c) 2015 UPenn. All rights reserved.
//

#ifndef __Motion_Path_Planner__mpAStar__
#define __Motion_Path_Planner__mpAStar__

#include <stdio.h>
#include "mpHeap.h"
#include <vector>
class mpAStarNode{
public:
    /*mpAStarNode();
    mpAStarNode(const mpAStarNode& other);
    mpAStarNode& operator = (const mpAStarNode& other);
    ~mpAStarNode();
    bool operator != (const mpAStarNode& other);
    bool operator == (const mpAStarNode& other);
    std::vector<mpAStarNode> GetNeighbors();
    float GetDistance (const mpAStarNode& other);
    float GetEstimate (const mpAStarNode& other);
    mpAStarNode* parentNode;*/
};

class mpAStar{
public:
   /* mpAStar();
    mpAStar(const mpAStar& other);
    mpAStar& operator= (const mpAStar& other);
    ~mpAStar();
    void findPath (mpAStarNode start, mpAStarNode goal, int maxDepth);*/
private:
    
};

#endif /* defined(__Motion_Path_Planner__mpAStar__) */
